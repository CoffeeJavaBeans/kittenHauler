import global from './global';
import buildConfig from './buildConfig';
import resize from './resize';
import preload from './preload';

//javascript resources for game

let main = { global, buildConfig, resize, preload };

export default main;
